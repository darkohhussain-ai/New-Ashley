
{
  "entities": {
    "Employee": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Employee",
      "type": "object",
      "description": "Represents an employee within the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Employee entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the employee."
        },
        "jobTitle": {
          "type": "string",
          "description": "The job title of the employee."
        },
        "employmentStartDate": {
          "type": "string",
          "description": "The date the employee started employment.",
          "format": "date-time"
        },
        "dateOfBirth": {
          "type": "string",
          "description": "The employee's date of birth.",
          "format": "date-time"
        },
        "email": {
          "type": "string",
          "description": "The employee's email address.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The employee's phone number."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL of the employee's photo.",
          "format": "uri"
        },
        "notes": {
          "type": "string",
          "description": "Notes or additional information about the employee."
        }
      },
      "required": [
        "id",
        "name",
        "jobTitle",
        "employmentStartDate",
        "dateOfBirth",
        "email",
        "phone"
      ]
    },
    "Expense": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Expense",
      "type": "object",
      "description": "Represents an expense incurred by an employee.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Expense entity."
        },
        "employeeId": {
          "type": "string",
          "description": "Reference to Employee. (Relationship: Employee 1:N Expense)"
        },
        "amount": {
          "type": "number",
          "description": "The amount of the expense."
        },
        "date": {
          "type": "string",
          "description": "The date the expense was incurred.",
          "format": "date-time"
        },
        "notes": {
          "type": "string",
          "description": "Notes or description of the expense."
        }
      },
      "required": [
        "id",
        "employeeId",
        "amount",
        "date"
      ]
    },
    "MonthlyReport": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MonthlyReport",
      "type": "object",
      "description": "Represents a monthly expense report for an employee.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the MonthlyReport entity."
        },
        "employeeId": {
          "type": "string",
          "description": "Reference to Employee. (Relationship: Employee 1:N MonthlyReport)"
        },
        "month": {
          "type": "string",
          "description": "The month the report is for (e.g., YYYY-MM).",
          "format": "date-time"
        },
        "totalExpenses": {
          "type": "number",
          "description": "The total expenses for the employee for the month."
        }
      },
      "required": [
        "id",
        "employeeId",
        "month",
        "totalExpenses"
      ]
    },
    "ArchivedExpenseList": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ArchivedExpenseList",
      "type": "object",
      "description": "Represents an archived expense list for a specific month.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ArchivedExpenseList entity."
        },
        "month": {
          "type": "string",
          "description": "The month the archived list is for (e.g., YYYY-MM).",
          "format": "date-time"
        },
        "expenseIds": {
          "type": "array",
          "description": "References to Expenses. (Relationship: ArchivedExpenseList 1:N Expense)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "month",
        "expenseIds"
      ]
    },
    "StorageLocation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StorageLocation",
      "type": "object",
      "description": "Represents a storage location.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the StorageLocation entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the location."
        },
        "warehouseType": {
          "type": "string",
          "description": "The type of warehouse (Ashley or Huana).",
          "enum": ["Ashley", "Huana"]
        }
      },
      "required": ["id", "name", "warehouseType"]
    },
    "ExcelFile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ExcelFile",
      "type": "object",
      "description": "Represents a created or imported Excel file.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ExcelFile entity."
        },
        "storekeeperId": {
          "type": "string",
          "description": "ID of the storekeeper employee."
        },
        "storageName": {
          "type": "string",
          "description": "Name of the storage."
        },
        "categoryName": {
          "type": "string",
          "description": "Name of the category for the imported items."
        },
        "date": {
          "type": "string",
          "description": "Date of the file.",
          "format": "date-time"
        },
        "source": {
            "type": "string",
            "description": "Source of the imported file.",
            "enum": ["Showroom", "Ashley Store", "Huana Store"]
        },
        "type": {
          "type": "string",
          "description": "Type of file (new or imported).",
          "enum": ["new", "imported"]
        }
      },
      "required": ["id", "storekeeperId", "date", "type", "storageName", "categoryName", "source"]
    },
    "Item": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Item",
      "type": "object",
      "description": "Represents an item within an Excel file.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Item entity."
        },
        "fileId": {
          "type": "string",
          "description": "ID of the parent ExcelFile."
        },
        "model": {
          "type": "string",
          "description": "The model of the item."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the item."
        },
        "notes": {
          "type": "string",
          "description": "Notes about the item."
        },
        "storageStatus": {
            "type": "string",
            "enum": ["Correct", "Less", "More"]
        },
        "modelCondition": {
            "type": "string",
            "enum": ["Wrapped", "Damaged"]
        },
        "quantityPerCondition": {
            "type": "number"
        },
        "locationId": {
            "type": "string"
        },
        "destinationCity": {
          "type": "string",
          "description": "The destination city for a transferred item.",
          "enum": ["Erbil", "Baghdad", "Diwan", "Dohuk"]
        },
        "transferId": {
          "type": "string",
          "description": "The ID of the transfer slip this item belongs to."
        }
      },
      "required": ["id", "fileId", "model", "quantity"]
    },
    "Transfer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transfer",
      "type": "object",
      "description": "Represents a cargo transfer slip.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Transfer entity."
        },
        "transferDate": {
          "type": "string",
          "format": "date-time",
          "description": "The date the transfer was created."
        },
        "cargoName": {
          "type": "string",
          "description": "A descriptive name for the cargo transfer."
        },
        "destinationCity": {
          "type": "string",
          "description": "The destination city for the transfer.",
          "enum": ["Erbil", "Baghdad", "Diwan", "Dohuk"]
        },
        "driverName": {
          "type": "string",
          "description": "The name of the driver."
        },
        "warehouseManagerName": {
          "type": "string",
          "description": "The name of the warehouse manager."
        },
        "itemIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "An array of item IDs included in this transfer."
        }
      },
      "required": [
        "id",
        "transferDate",
        "cargoName",
        "destinationCity",
        "driverName",
        "warehouseManagerName",
        "itemIds"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/employees/{employeeId}",
        "definition": {
          "entityName": "Employee",
          "schema": {
            "$ref": "#/backend/entities/Employee"
          },
          "description": "Stores employee profiles.  All employees are generally visible, so no specific authorization beyond authentication is needed.",
          "params": [
            {
              "name": "employeeId",
              "description": "Unique identifier for the employee."
            }
          ]
        }
      },
      {
        "path": "/expenses/{expenseId}",
        "definition": {
          "entityName": "Expense",
          "schema": {
            "$ref": "#/backend/entities/Expense"
          },
          "description": "Stores expenses for each employee. Uses path-based ownership based on `employeeId`.  This enables secure 'list' operations restricted to expenses owned by a specific employee. Contains `employeeId` to help with direct queries and filtering.",
          "params": [
            {
              "name": "expenseId",
              "description": "Unique identifier for the expense."
            }
          ]
        }
      },
      {
        "path": "/monthly_reports/{reportId}",
        "definition": {
          "entityName": "MonthlyReport",
          "schema": {
            "$ref": "#/backend/entities/MonthlyReport"
          },
          "description": "Stores monthly expense reports. Each document contains `employeeId` and the month for which the report is generated. This facilitates querying and aggregating monthly expenses per employee.",
          "params": [
            {
              "name": "reportId",
              "description": "Unique identifier for the monthly report."
            }
          ]
        }
      },
      {
        "path": "/archived_expense_lists/{archiveId}",
        "definition": {
          "entityName": "ArchivedExpenseList",
          "schema": {
            "$ref": "#/backend/entities/ArchivedExpenseList"
          },
          "description": "Stores archived expense lists. Each document contains `month` and an array of `expenseIds`. This allows for easy retrieval of archived expenses for a specific month.",
          "params": [
            {
              "name": "archiveId",
              "description": "Unique identifier for the archived expense list."
            }
          ]
        }
      },
      {
        "path": "/storage_locations/{locationId}",
        "definition": {
          "entityName": "StorageLocation",
          "schema": {
            "$ref": "#/backend/entities/StorageLocation"
          },
          "description": "Stores storage locations.",
          "params": [
            {
              "name": "locationId",
              "description": "Unique identifier for the storage location."
            }
          ]
        }
      },
      {
        "path": "/excel_files/{fileId}",
        "definition": {
          "entityName": "ExcelFile",
          "schema": {
            "$ref": "#/backend/entities/ExcelFile"
          },
          "description": "Stores metadata for created or imported Excel files.",
          "params": [
            {
              "name": "fileId",
              "description": "Unique identifier for the file."
            }
          ]
        }
      },
      {
        "path": "/excel_files/{fileId}/items/{itemId}",
        "definition": {
          "entityName": "Item",
          "schema": {
            "$ref": "#/backend/entities/Item"
          },
          "description": "Stores items within a specific Excel file.",
          "params": [
            {
              "name": "fileId",
              "description": "Unique identifier for the parent file."
            },
            {
              "name": "itemId",
              "description": "Unique identifier for the item."
            }
          ]
        }
      },
      {
        "path": "/transfers/{transferId}",
        "definition": {
          "entityName": "Transfer",
          "schema": {
            "$ref": "#/backend/entities/Transfer"
          },
          "description": "Stores transfer slips for cargo shipments.",
          "params": [
            {
              "name": "transferId",
              "description": "Unique identifier for the transfer."
            }
          ]
        }
      }
    ],
    "reasoning": "This Firestore structure is designed to manage employee data and expenses, incorporating key principles for security, scalability, and maintainability.  It leverages path-based ownership for employee data and employs denormalization to ensure authorization independence when managing expenses. The structure supports QAPs through structural segregation and predictable schema. Specifically:\n\n1.  **Employees Collection (`/employees/{employeeId}`):** Stores employee profiles. This top-level collection simplifies listing all employees. Path-based ownership is used, but since all employees are generally visible, no specific authorization is needed beyond authentication to access the employee list.\n\n2.  **Expenses Collection (`/expenses/{expenseId}`):** Each employee has their own expenses subcollection. The `employeeId` in the path grants implicit ownership. This structure enables secure 'list' operations restricted to a specific employee's expenses, fulfilling the QAPs requirement. The expenses collection also contains `employeeId` to help with direct queries, filtering, and denormalization for aggregation purposes.\n\n3.  **Monthly Reports Collection (`/monthly_reports/{reportId}`):** This collection stores monthly expense reports. Each document contains `employeeId` and the month for which the report is generated. This facilitates querying and aggregating monthly expenses per employee.\n\n4.  **Archived Expense Lists Collection (`/archived_expense_lists/{archiveId}`):** This collection stores archived expense lists. Each document contains `month` and an array of `expenseIds`. This allows for easy retrieval of archived expenses for a specific month."
  }
}

    